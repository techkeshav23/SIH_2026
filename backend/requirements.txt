fastapi==0.115.6
uvicorn[standard]==0.34.0
sqlalchemy==2.0.36
pydantic==2.10.4
pydantic-settings==2.7.1
python-jose[cryptography]==3.3.0
python-multipart==0.0.20
httpx==0.28.1
pillow==11.1.0
scikit-learn==1.6.0
numpy==2.2.1
pandas==2.2.3
joblib==1.4.2

# ---- infra: migrations, async jobs, prod DB driver ----
alembic==1.14.0
celery==5.4.0
redis==5.2.1
psycopg[binary]==3.2.3            # Postgres driver (prod)

# ---- AI ----
# Gemini powers voice cataloging (F2) + pricing reasoning (F3).
google-genai==1.9.0

# Image background removal (F1) — optional, heavier (onnxruntime + model download).
# Enable with USE_REMBG=true after installing. Default path uses Pillow (no key/deps).
# rembg==2.0.61

# ---- dev / test ----
pytest==8.3.4
