fastapi==0.115.6
uvicorn[standard]==0.34.0
sqlalchemy==2.0.36
pydantic==2.13.4          # >=2.11 required by google-genai 2.19 (Live API)
pydantic-settings==2.7.1
python-jose[cryptography]==3.3.0
python-multipart==0.0.20
httpx==0.28.1
pillow==11.1.0
scikit-learn==1.6.0
numpy==2.2.1
pandas==2.2.3
joblib==1.4.2

# ---- infra: migrations, async jobs, prod DB driver ----
alembic==1.14.0
celery==5.4.0
redis==5.2.1
psycopg[binary]==3.2.3            # Postgres driver (prod)

# ---- AI ----
# google-genai serves Gemini models via Vertex AI (prod/govt) or the Developer
# API. Vertex auth uses google-auth (bundled). Voice cataloging (F2) + pricing (F3).
google-genai>=2.19.0

# Durable product-image storage (Cloud Run disk is ephemeral).
google-cloud-storage==2.18.2

# Image background removal (F1) — true U2-Net cutout. Uses the lightweight
# u2netp model (~4.7MB, baked into the image). Enable with USE_REMBG=true.
# Falls back to Pillow automatically if inference fails (see image_ai.enhance).
rembg==2.0.61
onnxruntime==1.20.1

# ---- observability ----
sentry-sdk[fastapi]==2.19.2

# ---- dev / test ----
pytest==8.3.4
